import 'package:flutter/material.dart';

/// Detects when the teacher tab shell becomes visible again after a pushed route.
final RouteObserver<PageRoute<dynamic>> teacherRouteObserver =
    RouteObserver<PageRoute<dynamic>>();
